apiVersion: v1
kind: Service
metadata:
  name: {{ include "cloudflare-free-exporter.fullname" . }}
  labels:
    {{- include "cloudflare-free-exporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: metrics
      port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
  selector:
    {{- include "cloudflare-free-exporter.selectorLabels" . | nindent 4 }}
